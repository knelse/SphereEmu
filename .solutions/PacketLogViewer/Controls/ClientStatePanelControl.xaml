<UserControl x:Class="PacketLogViewer.Controls.ClientStatePanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListView Margin="4" x:Name="CurrentEntityStateForClient" x:FieldModifier="public" Background="AliceBlue">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding DisplayValue}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
            <Button x:Name="ClearClientStateButton" x:FieldModifier="public" Width="100" Height="30">
                Clear All
            </Button>
            <Button x:Name="FilterClientStateButton" x:FieldModifier="public" Width="100" Height="30"
                    Margin="0 6 0 0">
                Filter
            </Button>
        </StackPanel>

        <TextBox x:Name="ContentPreview" x:FieldModifier="public" Grid.Column="1" Margin="4 8 4 0"
                 HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" IsReadOnly="True"
                 Style="{StaticResource LogRecordContentPreviewTextStyle}" Background="LightGray" />
    </Grid>
</UserControl>

