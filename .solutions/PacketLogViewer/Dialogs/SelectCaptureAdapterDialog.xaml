<Window x:Class="PacketLogViewer.Dialogs.SelectCaptureAdapterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select network adapter"
        Width="980"
        Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,8" TextWrapping="Wrap">
            Select the network adapter to capture Sphere traffic from. The selected adapter's MAC address will be saved to appconfig.json.
        </TextBlock>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Adapters}"
                  SelectedItem="{Binding SelectedAdapter, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="MAC" Binding="{Binding MacAddress}" Width="160"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="220"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding DeviceType}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="2" Margin="0,10,0,0" LastChildFill="False">
            <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" Foreground="DimGray">
                Tip: pick the adapter that your game traffic goes through (Wi‑Fi/Ethernet).
            </TextBlock>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="110" Margin="0,0,8,0" IsCancel="True" Click="Cancel_OnClick">Cancel</Button>
                <Button Width="110" IsDefault="True" Click="Ok_OnClick">OK</Button>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
